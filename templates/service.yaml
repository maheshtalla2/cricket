apiVersion: v1
kind: Service
metadata:
  name: {{ include "cricketdata.fullname" . }}
  labels:
    {{- include "cricketdata.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cricketdata.selectorLabels" . | nindent 4 }}
