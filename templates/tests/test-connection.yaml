apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cricketdata.fullname" . }}-test-connection"
  labels:
    {{- include "cricketdata.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cricketdata.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
